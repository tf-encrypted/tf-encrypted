from setuptools import setup

setup(
    name = 'TensorflowEncrypted',
    version = '0.0.1rc',
    packages = ['tensorflow_encrypted',],
    license = '',
    long_description = open('README.md').read(),
)
